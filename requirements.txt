# === Desktop (todas as funcionalidades) ===
flet==0.82.2
supabase
httpx
pytz
python-dotenv
fpdf2
qrcode
pillow
anthropic

# Relatórios Desktop — instalar manualmente no desktop: pip install reportlab
# NÃO incluir no APK (buildozer não suporta compilação desta lib)
# reportlab

# OCR Desktop (opcional — instale manualmente se necessário)
# opencv-python
# pytesseract
# numpy

# Servidor de desenvolvimento (NUNCA incluir no APK)
# pyngrok
